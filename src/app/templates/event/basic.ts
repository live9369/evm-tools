import { CodeTemplate } from "../types";

export const basicTemplate: CodeTemplate = {
    id: "event-basic",
    name: "基础",
    description: "添加所有事件到卡片",
    color: "blue",
    code: `// 基础模板：添加所有事件到卡片
hooks.addEventCard({});`
};

